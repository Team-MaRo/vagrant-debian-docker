name: Release

# Conventional-commits → release PR → tagged release. release-please scans
# commits since the last release on master, opens (or updates) a "release"
# PR with the version bump + CHANGELOG.md, and on merge cuts a GitHub
# Release + git tag.
#
# The box itself is NOT built by CI: GitHub-hosted runners can't run x86
# VirtualBox, so building and publishing stay local. Once the release tag
# exists, run `bin/build.sh` then `bin/publish.sh <version>` on each native-arch
# host (amd64 on x86-64, arm64 on Apple Silicon) to push the box to the HCP
# Vagrant Registry.
#
# Version state lives in `.release-please-manifest.json`; release options go in
# `release-please-config.json`.

on:
  push:
    branches: [master]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Create release PR / GitHub Release
        uses: googleapis/release-please-action@v5
