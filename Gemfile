# frozen_string_literal: true

# Tooling-only Gemfile: provides RuboCop for linting the Vagrantfiles inside the
# devcontainer (see .devcontainer/devcontainer.json). It is NOT part of the box
# build — the box is built with Packer/Vagrant on the host, no Ruby project here.
source 'https://rubygems.org'

gem 'rubocop', require: false
